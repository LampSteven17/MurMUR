# Example sidecar env file for cluster.yaml.
# Copy to `cluster.env` (gitignored) next to your cluster.yaml.
# Murmur loads this file before parsing YAML and resolves ${VAR} references.

PROXMOX_TOKEN_SECRET=your-token-secret-here

# Optional: only needed if storage_backends references them
NAS_USER=
NAS_PASS=
